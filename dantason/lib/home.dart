import 'package:flutter/material.dart';

class MyHomePae extends StatelessWidget {
  const MyHomePae({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('Deu certo até aqui'),
      ),
    );
  }
}